## Database Configuration
DB_HOST=localhost
DB_PORT=27017
DB_USERNAME=pokemon
DB_PASSWORD=password
DB_DATABASE=pokemon_tcg

## Application Configuration
PORT=3000
NODE_ENV=production

## Cron Configuration
# Cron schedule for card sync (cron format)
# Examples:
#   - Every 6 hours: 0 */6 * * *
#   - Every hour: 0 * * * *
#   - Every 5 minutes (testing): */5 * * * *
CRON_SCHEDULE=0 */6 * * *

# Run sync immediately on application startup (true/false)
SYNC_ON_STARTUP=false
